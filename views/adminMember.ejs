<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://unpkg.com/mvp.css@1.12/mvp.css" />
    <script
      src="https://code.jquery.com/jquery-3.6.3.js"
      integrity="sha256-nQLuAZGRRcILA+6dMBOvcRh5Pe310sBpanc6+QBmyVM="
      crossorigin="anonymous"
    ></script>
    <style>
      .my_button {
        background-color: #cce1ff;
        color: white;
        border: none;
        padding: 8px 10px;
      }
    </style>
    <title>Document</title>
  </head>
  <body>
    <div style="text-align: center">
      <h1>회원 전체 조회</h1>
      <h2>
        <button>홈으로 돌아가기</button>
      </h2>
      <table id="table" class="table" border="1">
        <table>
          <th>ID</th>
          <th>계정</th>
          <th>닉네임</th>
          <th>비밀번호</th>
          <th>핸드폰</th>
          <th>이메일</th>
          <th>계정 생성일</th>
          <th>계정 업데이트</th>
          <th>권한</th>
          <% for(let i=0; i < members.length; i++) {%>
          <tr id="<%= members[i].userId %>">
            <td><%= members[i].userId %></td>
            <td><%= members[i].adminValid %></td>
            <td><%= members[i].userNickname %></td>
            <td><%= members[i].userPassword %></td>
            <td><%= members[i].userPhoneNumber %></td>
            <td><%= members[i].userEmail %></td>
            <td><%= members[i].userCreatedAt %></td>
            <td><%= members[i].userUpdatedAt %></td>
            <td>
              <button
                class="my_button"
                onclick="memberCommit(<%= members[i].userId %>)"
              >
                수정
              </button>
              <button
                class="my_button"
                onclick="memberDelete(<%= members[i].userId %>)"
              >
                삭제
              </button>
            </td>
          </tr>
          <%}%>
        </table>
      </table>
      <ul id="pagination"></ul>
    </div>
  </body>
  <script>
    document.querySelector("button").addEventListener("click", (e) => {
      window.location = "/api/admin/home";
    });

    function memberCommit(id) {
      location.href = `/api/admin/home/member/${id}`;
      console.log("memberCommit");
    }

    function memberDelete(id) {
      console.log("삭제버튼 클릭");
      // const tr = document.querySelector(`#${id}`);
      // console.log(tr);
      // tr.remove();
      $.ajax({
        url: `/api/admin/home/member/${id}`,
        type: "DELETE",
        data: { id: id },
        success: function (response) {
          console.log(response.message);
          alert("삭제되었습니다!");
          window.location.reload();
        },
        error: function (response) {
          console.error(error);
        },
      });
    }
  </script>
</html>
